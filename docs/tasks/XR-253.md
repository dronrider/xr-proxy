# XR-253: Цель: медиатека шары в приложении: двухстрочное имя вместо эллипса, метаданные файла с источником и каналом, заметный признак просмотрено с фильтром, группировка по дате и источнику

## Цель

Продолжение XR-249: проводник шары уже умеет сортировку, дату и признак
просмотрено, но владелец посмотрел экран списка файлов и назвал четыре
доработки.

1. Эллипс посередине имени решение неудачное: имя показывается в две строки, а
   компактность вида сохраняется, высота строки списка меняется незначительно.
   Экран предварительно дизайнится в Claude Design и согласуется с владельцем
   до реализации.
2. По видео не хватает информативности: понравившееся видео негде посмотреть,
   откуда оно скачано, и нет ссылки на страницу канала автора. Нужны
   элементарные метаданные файлов медиатеки и экран информации о файле, где
   видно всё, что о нём известно. Импорт по URL (LLD-29) пишет метаданные сам,
   а по уже скачанным файлам делается разовое дозаполнение тем, что удаётся
   вытащить.
3. Глазик просмотренности серый и почти незаметен; кроме того, отобрать одни
   непросмотренные нельзя.
4. Нет группировки записей, нужен компактный переключатель режимов отображения
   как в файловых проводниках: группировка по дате и группировка по источнику
   (у видео это канал), плюс фильтр по просмотренным.

DoD: каждый пункт закрыт агентским экранным сценарием на эмуляторе (AVD
xr-test, uiautomator, порядок в памяти проекта). Длинное имя файла видно в две
строки без эллипса посередине, высота строки списка выросла не больше чем на
треть; у импортированного видео открывается экран информации с источником и
ссылкой на канал, а у файла, скачанного до этой цели, тот же экран показывает
дозаполненное либо честно пустое поле; просмотренный файл отличим от
непросмотренного в списке, а фильтр оставляет только непросмотренные;
переключатель режимов собирает список группами по дате и группами по
источнику. После зелёных сценариев цикл останавливается и зовёт владельца на
проверку на телефоне; закрывает цель владелец.

Дизайн двухстрочной строки списка и экрана информации о файле цикл сам не
утверждает: макет уходит в Claude Design, и виток выходит `wait-human` до
ответа владельца.

## Бюджет

бюджет: week_all <= 15
бюджет: week_max <= 10

Ярус не ограничен. Бакеты общие на машину: числа это верхняя граница трат
машины за время цикла, а не точный счётчик цели.

## Задачи цели

- XR-254: дизайн проводника в Claude Design, один макет на все четыре пункта
  постановки, чтобы владельца звать один раз, а не на каждый экран. Кончается
  согласованием, дальше реализация идёт по утверждённому. Закрыта: владелец
  выбрал вариант Б признака просмотра, точку у непросмотренного в левом
  отступе строки. Остальное в макете принято как есть.
- XR-255: метаданные файлов шары. Пишет их агент при импорте, отдаёт клиенту
  вместе с листингом, по уже скачанным файлам разовое дозаполнение. Фундамент
  для XR-257 и XR-258, едет параллельно дизайну. Закрыта, агент опубликован
  тегом xr-share-v0.3.8 на оба хаба.
- XR-256: строка списка, пункты 1 и 3 постановки. Закрыта.
- XR-257: экран информации о файле, пункт 2. Закрыта.
- XR-258: группировка и переключатель режимов, пункт 4. Закрыта. Зависимость от
  XR-256 по ходу снята: она стояла не по смыслу, а чтобы две ветки не толкались
  в шапке списка, и после слияния XR-256 в main причина исчерпалась.

Все три уехали одним релизом APK 0.102.0 на оба хаба.

## Журнал

- снимок 2026-08-04T21:30: week_all 18%, week_max 11%
- 2026-08-04, нарезка: 5 задач XR-254..XR-258, порядок по dep; дизайн и
  метаданные независимы и едут первой парой; continue
- 2026-08-04, XR-255 закрыта по агентскому сценарию, агент выкачен тегом
  xr-share-v0.3.8 на оба хаба; XR-254 в Check, макет ждёт владельца, и три
  оставшиеся задачи цели держит его ответ; wait-human
- снимок 2026-08-04T22:45: week_all 19%, week_max 11%
- 2026-08-04, владелец выбрал вариант Б признака, XR-254 закрыта; XR-256
  написана, но экранный сценарий встал: с машины пропало всё Android-окружение
  (~/android-tools, AVD xr-test со снапшотом xr-ready, gradle-кэш), снесено
  около 14:03 не нами. Восстановлено xr-android/setup.sh плюс доставленные
  эмулятор, платформа 35 и системный образ, AVD пересобран; стенд для сценариев
  теперь локальный (свой хаб и агент, боевых данных не нужно) и закреплён в
  local-docs/android-stand, снапшот xr-ready пересобран; continue
- 2026-08-05, XR-256 прошла сценарий на эмуляторе и слита, но оставлена в
  поезде: релиз APK один на все экранные задачи цели, а не три подряд. Замер
  показал, что обещание макета «высота строки не растёт» не сбылось, строка с
  длинным именем выросла с 54.1dp до 60.6dp, то есть на 12% при пороге в треть.
  Находка про онбординг на локальном стенде заведена строкой XR-259; continue
- снимок 2026-08-04T23:28: week_all 20%, week_max 12%
- снимок 2026-08-05T00:16: week_all 21%, week_max 12%
- 2026-08-05, XR-257 и XR-258 прошли сценарии и слиты, поезд выкачен релизом
  APK 0.102.0 на оба хаба, все три задачи закрыты. Ревью XR-258 поймало
  настоящий дефект: группа «без источника» ключевалась строкой «none», и канал
  с таким именем терял место в конце списка; заодно нашёлся дубль ключа
  заголовка, роняющий список. Оба закрыты тестом на чистую логику
  группировки. Цель ждёт проверки владельцем на телефоне; wait-human

## Итог

Все пять задач цели закрыты, обе доработки уехали на прод: агент шары
`xr-share-v0.3.8` (метаданные файлов) и приложение `0.102.0` (три экранные
правки). Потрачено 3 процентных пункта `week_all` и 1 `week_max` при потолке
15 и 10.

По пунктам постановки:

1. Имя файла показывается в две строки целиком, эллипс посередине убран.
   Обещание макета «высота строки не растёт вовсе» не сбылось: строка с длинным
   именем выросла с 54.1dp до 60.6dp, на 12% при пороге цели в треть. Короткая
   строка не изменилась.
2. Метаданные файлов пишет агент при импорте и разово дозаполняет по уже
   скачанным; экран информации о файле показывает источник и канал ссылками, а
   у файла без метаданных честно говорит, что источник неизвестен.
3. Признак просмотра поменял смысл: метится непросмотренное точкой цвета
   primary, серый глазок убран. Фильтр «Только непросмотренные» живёт в меню
   вида и переживает перезапуск.
4. Группировка «Без групп», «По дате», «По источнику» в том же меню, со
   счётчиками в заголовках. Папки в группы не ложатся и стоят блоком сверху:
   ни даты, ни источника у них нет, а прятать их нельзя.

Проверено на выкаченном APK, скачанном с хаба: обновление встало поверх
установленного без переустановки, все четыре экрана сняты деревом uiautomator.

Что цель не закрыла и что стоит знать:

- метаданные начнут копиться только после того, как владелец обновит агента
  шары на своей машине (`irm https://xr-hub.zoobr.top/share/install.ps1 | iex`),
  и допишет новые аргументы yt-dlp в уже настроенный блок `[import]`;
- по ходу заведены две строки: XR-259 (онбординг по ссылке не проходит на
  локальном стенде) и XR-260 (импорт по URL из приложения на стенде не доходит
  до агента);
- Android-окружение машины было снесено извне посреди цели и восстановлено
  заново; стенд для экранных сценариев теперь свой, локальный, и описан в
  `local-docs/android-stand/README.md`.
