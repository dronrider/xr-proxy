# XR-279: Соединение держится до явного отключения пользователем: сейчас туннель не возвращается ни после убийства процесса системой, ни после перезагрузки телефона, ни после свайпа из недавних, и пользователь остаётся без защиты, не зная об этом

## Симптом

Пользователь включил VPN и убрал телефон в карман. Дальше любое из трёх событий
оставляет его без туннеля, и на экране это выглядит как «я сам выключил»:

1. система выгружает процесс под нехватку памяти;
2. телефон перезагружается;
3. приложение уходит свайпом из недавних.

Ни в одном случае туннель сам не поднимается. Трафик идёт напрямую с реальным
IP, а приложение показывает `Idle`, визуально неотличимый от намеренного
отключения. Ровно тот же приватностный разрыв, который разбирали в XR-132, но
там его чинили внутри живого сервиса, а тут сервиса уже нет.

Ожидаемое поведение одно на все три случая: соединение держится, пока
пользователь сам не решил его разорвать.

## Разбор текущего состояния

- [XrVpnService.onStartCommand](../../../../xr-android/app/src/main/java/com/xrproxy/app/service/XrVpnService.kt#L291-L315)
  возвращает `START_STICKY`, но пересозданный системой сервис приходит с
  `intent == null` и намеренно делает `stopSelf()`: конфига у него нет, а
  поднимать туннель вслепую нечем. Это и есть точка правки первого случая.
- Ресивера `BOOT_COMPLETED` в манифесте нет, разрешения тоже, автостарта после
  перезагрузки не существует.
- Желаемое состояние («пользователь хочет быть подключён») нигде не хранится.
  В `SharedPreferences` лежат доверенные сети, серверы и правила, признака
  «туннель должен стоять» среди них нет.
- `onTaskRemoved` не переопределён, поведение при свайпе из недавних на живых
  устройствах не проверялось.

## Развилки

- Штатный always-on VPN системы против своего автостарта. Системный делает всё
  сам, включая lockdown, но включается только руками в настройках Android и
  программно не выставляется. Скорее всего нужны оба: свой автостарт как
  поведение по умолчанию и подсказка про always-on для тех, кому нужен lockdown.
  Решение: свой автостарт, подсказку в UI не заводили. Экран настроек под
  всегда-включённый режим никто не просил, а включить его может только
  человек в настройках системы.
- Что именно сохранять для восстановления: весь стартовый конфиг движка или
  ключ профиля с пересборкой конфига из `PresetCache`. Второе переживает смену
  пресета, но требует, чтобы кеш был готов до сети.
  Решение: весь конфиг целиком. Пресет и правила пользователя движок
  перечитывает с диска на каждом старте, восстановленная сессия выходит
  свежей. Путь через ключ профиля требовал бы готовности кеша до сети. Смена
  активного сервера всегда идёт через новый Connect, сохранённый конфиг
  обновляется тем же заходом.
- Стык с доверенными сетями: восстановление обязано уважать паузу на доверенной
  сети, иначе после перезагрузки дома туннель встанет там, где его штатно
  снимают.
  Решение: восстановление входит в тот же `startVpn`, что и живой коннект,
  начальное ожидание SSID и авто-пауза работают без отдельного кода. Override
  «включить здесь» сохраняется рядом с флагом и восстанавливается вместе
  с ним. Без этого туннель, поднятый вручную на доверенной сети, после
  перезагрузки уезжал бы в авто-паузу.
- Стык с XR-132: восстановление это вход в ту же машину состояний, что и
  авто-резюм, и падать оно должно в `Reconnecting`, а не в `Idle`.
  Решение: LLD-34 (фаза `Reconnecting`) до сих пор не реализован. Окно
  ретрая живёт только у восстановления, до первого подтверждённого
  Connected или паузы, снаружи виден как Connecting. Провал окна (нет сети
  после перезагрузки) повторяется с backoff 5с -> 60с и не гасит сессию.
  Смерть уже поднятого туннеля остаётся территорией LLD-34.
- Отличить своё отключение от системного: `stopFromUi` и `onRevoke` (XR-221)
  уже разведены, флаг желаемого состояния гасит только первое.
  Решение: так и сделано. За это отвечает `clearsWantedOnStop` и юнит на
  него. Системный отзыв оставляет флаг, и после перезагрузки туннель
  возвращается.
- Ограничения фонового старта: `startForegroundService` из `BOOT_COMPLETED`
  разрешён, но поведение проверяется на живом устройстве, не по документации.
  Решение: ресивер стоит, оба действия (`BOOT_COMPLETED`,
  `MY_PACKAGE_REPLACED`) входят в исключения запуска foreground-сервиса, тип
  сервиса уже объявлен. Живая проверка осталась в сценарии. Самообновление
  APK добавлено четвёртым случаем воскрешения. Канал обновлений у приложения
  свой (LLD-12), без ресивера туннель возвращался бы только со следующего
  ребута. Прогон на эмуляторе вскрыл второе ограничение, в документации не
  очевидное. На Android 14+ фоновый старт не имеет права на while-in-use тип
  location, платформа его срезает. Первая правка убрала location из фоновых
  стартов (решение `RestorePolicy.foregroundLocationTypeAllowed`), но прогон
  0.106.0 показал, что туннель это не вернуло. Вторая версия винила тип
  systemExempted: строки «does not have any types» в логе прочитались как
  срезание обоих типов, и базовым типом стал specialUse. По исходникам AOSP
  читка оказалась ошибкой. Эти строки пишет наблюдательный модуль
  ForegroundServiceTypeLoggerModule про отсутствие while-in-use типов, и у
  чистого specialUse они есть и при успехе. Старт сервиса во всех трёх
  провалах система разрешала, гасил себя сам сервис. На шестой секунде
  после загрузки establish() возвращает null. Слот подготовленного VPN
  при перезагрузке гибнет, переживает только согласие, оно лежит в appop.
  Выставляет слот заново ленивый prepare(), а путь восстановления его
  не звал.
  specialUse остался базовым типом как честный для стороннего VPN.
  Несущий фикс это prepare() перед establish(). Тип location
  по-прежнему получает только старт из UI с выданным разрешением.
  Плата за это: SSID восстановленной
  сессии закрыт до первого открытия приложения. Авто-пауза доверенной сети
  ждёт этого открытия, туннель сам стоит.

## Тесты

Чистая Kotlin-логика (решение «надо ли восстанавливать» по сохранённому
состоянию, причине остановки и вердикту доверенной сети) идёт JVM-юнитами в
`xr-android/app/src/test/`. Три случая из симптома проверяются на эмуляторе.

- Исключение: тесты (юниты в ветке есть, это `RestorePolicyTest.kt` с
  девятью тестами, но ворот слияния узнаёт тесты по суффиксам
  `_test.go/.py/.sh`, а Kotlin-овский `Test.kt` не видит).

## Приёмка

- 2026-08-28, вниз (agent -> user): эмулятор xr-test, на котором сценарий
  планировался машинным, недоступен, проверка уезжает на живое устройство.
- 2026-08-29, вверх (user -> agent): USB-стек мака ожил без перезагрузки,
  adb отвечает, эмулятор снова машина. Барьер «доступ» снялся: сценарий
  прогнан субагентом на эмуляторе, живой телефон остаётся дополнением
  для OEM-случаев свайпа и Doze.

- барьер «доступ»: системные события сценария (смерть процесса,
  перезагрузка, свайп) вызываются только на живом Android-устройстве, а
  такого у агента нет. Единственный стенд, эмулятор xr-test, не
  поднимается из-за повешенного USB-стека мака: adb-сервер виснет в
  usb_init на platform-tools 37.0.1 и 33.0.3, USB-план ioreg пуст.
  - Поднять эмулятор на этом маке нельзя. USB-стек повешен системно и
    лечится только перезагрузкой машины. Исход: стенд недоступен до
    решения владельца.
  - Поднять эмулятор на другой машине нельзя. Второй машины с рабочим
    Android-стендом рядом нет, заводить её дольше, чем проверить на живом
    телефоне. Исход: остаётся живое устройство.
  - Перезагрузить мак может только владелец. Это рабочая машина, решение о
    перезагрузке за ним. Исход: ждать перезагрузку в задаче нечего.
  - Программный путь без устройства (JVM-юниты, сборка APK) агент прогнал,
    но системные события юнитом не доказать. Исход: юниты идут дополнением
    к живой проверке, не вместо неё.

## Ход работы

- Разработка: подпроцесс glm-5.3/high по вердикту pick (квота: window5h_all 0%, week_all 0% профицит, снимок 32м назад, сдвига нет), 2026-08-28 13:12-13:14.
- Код: `RestorePolicy.kt` (чистые решения о восстановлении), `WantedSessionRepository.kt` (хранение желания, конфига и override), `XrVpnService` (вход восстановления `restoreSession`, окно ретрая `handleSessionFailure`, персист override, `onTaskRemoved` со следом в журнале), `BootCompletedReceiver` + манифест (разрешение `RECEIVE_BOOT_COMPLETED`, действия `BOOT_COMPLETED` и `MY_PACKAGE_REPLACED`).
- Тесты: `cd xr-android && JAVA_HOME=$HOME/android-tools/jdk17 ./gradlew :app:testDebugUnitTest` зелёный (`RestorePolicyTest`, 8 юнитов), `PATH=$HOME/.cargo/bin:$PATH cargo test --workspace` зелёный (839 тестов, без предупреждений), отладочный APK собирается `build.sh`.
- Эмулятор для проверки недоступен: USB-стек мака повешен системно, след
  пересмотра вида приёмки выше в «Приёмке».
- Ревью: подпроцесс glm-5.3/high по вердикту pick (квота: window5h_all 33%, week_all 7% профицит, снимок 5м назад, сдвига нет), 2026-08-28 14:05-17:33.
- Релиз APK для проверки: `PATH=$HOME/.cargo/bin:$PATH scripts/release-apk.sh --bump` поднял 0.105.0 (code 105, sha256 dd4d26f3238e02f215638fcb688f2f6f9171f40f5b09d8cdfdaec28fe8603bce) поверх живого 0.104.0, `latest` и download проверены на обоих хабах, прежние манифесты в бэкапах manifest.{json,sig}.bak.20260828-230605, 2026-08-28.
- Снаружи: проверка после выката, 2026-08-28 17:33-16:50.
- Второй круг, правка по итогам проверки: тип location у foreground-старта
  получает только старт из UI (флаг `sessionFromUi`, решение
  `RestorePolicy.foregroundLocationTypeAllowed`, юнит на него), фоновые
  восстановления идут одним systemExempted, 2026-08-29. Прогон после правки:
  юниты зелёные (`JAVA_HOME=$HOME/android-tools/jdk17 ./gradlew
  :app:testDebugUnitTest`, 9 тестов `RestorePolicyTest`). Воркспейс зелёный
  без предупреждений (`PATH=$HOME/.cargo/bin:$PATH cargo test --workspace`).
- Ревью второго круга: подпроцесс glm-5.3/high по вердикту pick (квота:
  window5h_all 56%, week_all 49% профицит, снимок 11м назад, сдвига нет),
  2026-08-29. По коду и докам замечаний нет, два процессных закрыты ниже.
- Ревью: подпроцесс glm-5.3/high по вердикту pick (квота: window5h_all 56%, week_all 49% профицит, снимок 11м назад, сдвига нет), 2026-08-29 16:59-17:24.
- Релиз APK второго круга: `PATH=$HOME/.cargo/bin:$PATH scripts/release-apk.sh --bump` поднял 0.106.0 (code 106, sha256 7f7a1918e02cb7fb15045f5112e389af16e0e1d209d9f8f6a1bb6d8b0d41ab58) поверх живого 0.105.0, `latest` и download проверены на обоих хабах, прежние манифесты в бэкапах manifest.{json,sig}.bak.20260829-172511, 2026-08-29.
- Снаружи: проверка после выката, 2026-08-29 17:24-18:10.
- Третий круг, 2026-08-29: тип specialUse вместо systemExempted (манифест, пермишен, свойство подтипа, рантайм), прогон субагентом пяти шагов с полным logcat, коммит 1c8dfa4c.
- Четвёртый круг, 2026-08-29: по исходникам AOSP и опыту проб найден несущий механизм. Слот подготовленного VPN при перезагрузке гибнет, переживает только согласие в appop, а выставляет слот заново ленивый prepare(). Правка: bringTunnelUp зовёт prepare() перед establish(). Тексты о срезании типов переписаны (здесь, ARCHITECTURE, KDoc, манифест). Юниты зелёные (9 тестов RestorePolicyTest), контрольный прогон на отладочном APK code 1909 закрыл шаг перезагрузки.
- Ревью: подпроцесс glm-5.3/high по вердикту pick (квота: window5h_all 48%, week_all 67% профицит, снимок 10м назад, сдвига нет), 2026-08-29 20:16-20:42. Замечания 9-11 текстовые, закрыты правками, финальный вердикт без замечаний.
- Слияние: fast-forward, код запушен, выкат прошёл, XR-279 в Check (коммит 14fdbd19), 2026-08-29.
- Релиз APK четвёртого круга: `PATH=$HOME/.cargo/bin:$PATH scripts/release-apk.sh --bump` поднял 0.107.0 (code 107, sha256 94701479c9282ebcfe2f634913c9d725706bac41a46f37bf5b54706cc502f45a) поверх живого 0.106.0, `latest` и download проверены на обоих хабах, прежние манифесты в бэкапах manifest.{json,sig}.bak.20260829-204249, 2026-08-29.
- Снаружи: проверка после выката, 2026-08-29 20:42-20:49.

## Ревью

- XrVpnService.kt: restoreRetryJob без @Volatile, поле пишется из
  корутины провала, а гасится с main. Явное отключение в окне ретрая могло
  не отменить отложенный старт.: исправлено, @Volatile на restoreRetryJob,
  тело повтора проверяет restorePending перед startVpn, коммит 28402411.
- Шаг 1 сценария вёл к force-stop («Остановить» в сведениях о приложении),
  после которого туннель по замыслу не возвращался, шаг ложно провалился
  бы.: исправлено, шаг переписан на `adb shell am crash com.xrproxy.app`,
  предупреждения про force-stop и `am kill` добавлены.
- restoreSession отсекал повторный вход только по фазе. Sticky-рестарт
  и ACTION_RESTORE подряд, до первого publish(Preparing), поднимали второй
  TUN поверх живого.: исправлено, пропуск дубля вынесен в
  `RestorePolicy.duplicateRestoreSkipped(phaseBusy, restorePending)`,
  накрыт юнитом.
- ARCHITECTURE.md: «гасит флаг только stopFromUi» неточно, код гасит
  желание ещё при отвергнутом движком конфиге.: исправлено, пункт 8
  называет оба случая гашения флага.
- У первого обхода барьера «железо» не названа причина.: исправлено,
  причина дописана, второй машины с рабочим Android-стендом рядом нет.
- «Ход работы» называл юниты зелёными без команды прогона.: исправлено,
  команда вписана, после правок прогнано заново, 8 юнитов зелёные.
- Файл задачи, раздел «Тесты» называл юнитов восемь при девяти, запись второго круга в «Ход работы» шла без прогона.: исправлено, счётчик сверен с XML прогона (tests="9"), команда и итог дописаны в запись второго круга
- Коммит ветки с правкой файла задачи не уехал в origin сразу.: отклонено, пуш ветки с кодом из агентской сессии режется pre-push хуком devkit, проходит только дифф из доски; доска уезжает в origin слиянием shipctl merge в автономном режиме, отдельный пуш ветки ничего не добавляет
- docs/tasks/XR-279.md, «Развилки»: «По исходникам AOSP чтка оказалась ошибкой»: потеряна буква «и», надо «читка».: исправлено, опечатка исправлена, вдогонку в этом же абзаце переписан устаревший вывод про окно повтора
- Сценарий проверки озаглавлен «Пользовательский, на живом телефоне», а вид приёмки агентский и прогон шёл субагентом на эмуляторе, шаг 1 внутри сам предлагает SIGKILL на эмуляторе с root. Заголовок переписать под агентский вид: эмулятор основная машина, живой телефон дополнение для OEM-случаев свайпа и Doze.: исправлено, заголовок переписан под агентский вид: эмулятор основная машина, живой телефон дополнение для OEM-случаев
- docs/tasks/XR-279.md, «Развилки»: «Слот подготовленного VPN переживает перезагрузку только в appop согласия» переворачивает механизм: переживает перезагрузку только согласие, сам слот в системе гибнет, prepare() выставляет его заново. Сформулировать точно, как в ARCHITECTURE и в «Проверке».: исправлено, фраза перевёрнута была, теперь слот гибнет, согласие в appop переживает, prepare выставляет слот заново

## Сценарий проверки

Агентский, основная машина эмулятор с root (прогоны этого круга шли на
xr-test, API 35), свежий APK ставится поверх живого. Живой телефон остаётся
дополнением для OEM-случаев свайпа и Doze. До начала туннель подключён,
экран приложения закрыт.

1. Смерть процесса. Убить процесс удобнее крашем, равнозначным выгрузке
   под нехватку памяти: `adb shell am crash com.xrproxy.app`.
   Система перезапускает sticky-сервис с пустым intent, туннель поднимается
   сам, в журнале приложения строка «восстановление после перезапуска
   процесса». Статус Connected, трафик идёт через прокси (проверить своим
   способом, например по exit-IP). Кнопка «Остановить» в сведениях о
   приложении сюда не годится: force-stop гасит и ресиверы, туннель после
   него не возвращается намеренно. `am kill` тоже не годится,
   foreground-процесс сервиса он не трогает. На Android 14+ краш кладёт
   в сервис счётчик сбоев, и система откладывает sticky-рестарт на полчаса
   (наблюдалось на эмуляторе API 35). Шаг на таких телефонах либо ждёт это
   окно, либо проверяется перезагрузкой из шага 2: путь восстановления тот
   же. Честный эквивалент выгрузки это чистый SIGKILL, на эмуляторе с root
   он и применяется (`adb shell su 0 kill -9 <pid>`).
2. Перезагрузка телефона. Перезагрузить устройство и не открывать
   приложение. После загрузки сети туннель возвращается сам, в журнале
   строка «восстановление после загрузки телефона», статус Connected. Если
   сеть после загрузки появилась не сразу, в журнале видны повторы с ростом
   паузы и итоговый подъём.
3. Свайп из недавних. С подключённым туннелем убрать приложение свайпом из
   недавних. На стоковом Android foreground-сервис переживает свайп, в
   журнале строка «задача убрана из недавних, сервис продолжает работать»,
   туннель стоит. Если прошивка процесс всё же убила, работает путь пункта 1
   и туннель возвращается сам.
4. Отключение гасит всё. Нажать «Отключить» в приложении, затем перезагрузить
   телефон. Туннель сам не поднимается, статус Idle. Без этого шага
   получился бы туннель, который нельзя выключить.
5. Как раньше и не сломалось. Обычное подключение и отключение из
   приложения, пауза на доверенной сети и выход из неё, «включить здесь»
   ведут себя как до правки.

## Выкат

- 2026-08-28 слито: d60c9b56, c9eed01b, b23cdccd, ef587b1a
- 2026-08-29 слито: 70e04315, 7634e6a3
- 2026-08-29 слито: a44e661c, 07aeef69, 12e46b90, 2f6f1aa3, c79a11e2, a5ccbd8a, cb9dfb64, 72287930
- smoke прогнан, 2026-08-29

## Проверка

- 2026-08-29, эмулятор xr-test (API 35), APK 0.105.0 из release-staging, стенд-сервер xr-server на 127.0.0.1:9443. Прошли: установка без ложного автостарта (MY_PACKAGE_REPLACED поднял процесс, туннель не тронул), подключение через UI, свайп из недавних (процесс жив, tun0 стоит, мукс-сессии живы), «Отключить» гасит всё. После «Отключить» и ребута туннель сам не поднимается, в журнале «туннель остановлен пользователем».
- Провал, шаг перезагрузки, 2 повтора из 2. Ресивер BOOT_COMPLETED сработал, restoreSession вошёл. В журнале «восстановление после загрузки телефона: поднимаю сохранённое подключение». Дальше establish() вернул null («Не удалось поднять TUN»), ретрая для TUN_ESTABLISH нет. Системный лог в ту же секунду: «Foreground service started from background can not have location/camera/microphone access», «FGS start for UID 10207 does not have any types». Сервис стартовал с типом systemExempted|location. На Android 14+ фоновый старт с while-in-use типом location платформа срезает, тип падает в NONE, VPN-establish ломается. Не-UI старты (adb RESTORE, adb START фоном) падают так же, UI-старты работают. Ставит тип startForegroundWithLocationType: добавляет FOREGROUND_SERVICE_TYPE_LOCATION по факту разрешения, происхождение старта не различает.
- Артефакт метода, шаг смерти процесса. am crash на API 34+ кладёт crashCount, рестарт sticky-сервиса откладывается на 30 минут (service_crash_restart_duration). Null-intent за окно сценария не приходит. Честный эквивалент выгрузки по памяти это чистый SIGKILL. Прогон после отравленного crashCount невалиден, шаг повторить чистым SIGKILL.
- Свайп из недавних: функция держится, сервис пережил, туннель стоит. Строки «задача убрана из недавних, сервис продолжает работать» в журнале нет, событий wm_task_removed образ не доставил. След журнала на этом образе не приходит, функциональная проверка остаётся основной.
- Исход: задача возвращается в работу. Чинить фоновый старт (тип location только у стартов из UI), затем повторный прогон шагов с чистым SIGKILL и перезагрузкой.
- Повторный прогон, 2026-08-29, тот же стенд, APK 0.106.0 (code 106, sha256 7f7a1918e02cb7fb15045f5112e389af16e0e1d209d9f8f6a1bb6d8b0d41ab58). Прошли: установка без ложного автостарта, подключение из UI, свайп из недавних (на этом образе пришла и строка «задача убрана из недавних, сервис продолжает работать»), «Отключить» гасит всё с ребутом, обычный цикл подключения.
- Провал, шаг перезагрузки, 1 повтор из 1, тот же симптом. В журнале «восстановление после загрузки телефона: поднимаю сохранённое подключение», следом «Не удалось поднять TUN». Системный лог: «Background started FGS: Allowed (BOOT_COMPLETED)», затем «Foreground service started from background can not have location/camera/microphone access», «FGS start for UID 10207 does not have any types», «FGS stop call for: 10207 has no types». Разрешение ACCESS_FINE_LOCATION на стенде не выдано вовсе, в рантайм тип location не передавался, передавался один SYSTEM_EXEMPTED. Правка второго круга (происхождение старта) на симптом не повлияла.
- Провал, шаг смерти процесса, чистый SIGKILL, 2 повтора. Процесс не ожил ни за 90с, ни за 13 минут наблюдения: «Scheduling restart of crashed service» в logcat нет, ServiceRecord показывает startCommandResult=1 (START_STICKY), рестарт не исполнен. На 0.105.0 шаг по постановке проходил, поведение требует разбора.
- Исход повтора: правка второго круга закрыла не тот механизм, срезание типов у фонового старта на этом образе смотрит не на набор типов вызова startForeground. Задача снова в работе: нужен настоящий механизм срезания и отдельный разбор отсутствия sticky-рестарта после SIGKILL на API 35.
- Третий круг, 2026-08-29, тот же стенд, отладочный APK с правкой specialUse (коммит ветки 1c8dfa4c). Прошли: подключение из UI с типом 0x40000000 (SPECIAL_USE в dumpsys), свайп из недавних (строка «задача убрана из недавних, сервис продолжает работать» пришла), замена пакета при живом туннеле (MY_PACKAGE_REPLACED поднял восстановление, тип выжил, стенд принял сессии), «Отключить» гасит всё с контрольным ребутом. Полный отчёт /tmp/xr279-run3-report.md.
- Провал, шаг перезагрузки, третий раз с тем же симптомом. Читка системного лога сменилась по исходникам AOSP. Строки «does not have any types» и «FGS stop call has no types» пишет наблюдательный модуль ForegroundServiceTypeLoggerModule. Они значат, что у сервиса нет while-in-use типов для отслеживания, и появляются у чистого specialUse даже при успешном старте. Предупреждение про location/camera/microphone в ту же секунду получил и Google Messaging. Старт FGS из BOOT_COMPLETED система разрешает, сервис встаёт с типом. Через 38 мс он гасит себя сам. Establish() вернул null, TUN_ESTABLISH объявлен нелечимым, ретрая нет.
- Настоящий механизм провала перезагрузки. Попытка восстановления приходит на шестой секунде после загрузки, establish() возвращает null. По исходникам AOSP (Vpn.java, establish) null здесь молчаливый и без временного окна. Слот подготовленного пакета живёт в системе только до перезагрузки. Согласие пользователя это персистентный appop ACTIVATE_VPN, он ребут переживает, а слот возвращается приложению лениво, очередным prepare() из приложения. Единственный prepare() в коде стоял в UI (VpnViewModel), путь восстановления его не звал. Опыт пробами RESTORE закрыл и версию с окном готовности. Пробы на +45/+120/+240/+420/+600с после загрузки все красные (лог /tmp/xr279-boot-horizon.log). Тёплые старты проходили потому, что слот не сбрасывался. Успех на +12 минуте во втором круге дал открывавшийся перед пробой UI приложения своим prepare(). Несущий фикс: bringTunnelUp зовёт prepare() перед establish(). Ненулевой intent означает честный отзыв согласия и идёт прежним путём TUN_ESTABLISH без повтора.
- Провал, шаг смерти процесса, третье измерение того же. После чистого SIGKILL рестарт системой не назначен вовсе, в карточке сервиса нет nextRestartTime, счётчик сбоев чистый, START_STICKY записан честно. По AOSP рестарт обязан назначаться за секунды, образ Google ведёт себя иначе. Вопрос вынесен в разбор сценария.
- Четвёртый круг, 2026-08-29, тот же стенд, отладочный APK code 1909 с prepare() перед establish(). Прошли все шаги, включая тот, что падал три круга. Через 5с после boot_completed журнал дал «восстановление после загрузки телефона: поднимаю сохранённое подключение» и «туннель поднят», на пробе +10с tun0 стоит, pid живого процесса 2660. Замена пакета при установке тоже подняла восстановление сама. Свайп из недавних сервис пережил, туннель стоит. «Отключить» гасит всё с контрольным ребутом, как в третьем круге. Лог прогона /tmp/xr279-reboot-verify.log.
- Шаг смерти процесса на этом образе остаётся платформенным. Три измерения на API 35 показали, что система не назначает рестарт убитому foreground-сервису вопреки START_STICKY. Код пути готов: null-intent входит в restoreSession, prepare() вернёт слот, establish пройдёт. На прошивках, где рестарт назначается, туннель вернётся.
- Контрольный прогон релиза, 2026-08-29, тот же стенд, выкаченный с хаба 0.107.0 (code 107, sha256 94701479c9282ebcfe2f634913c9d725706bac41a46f37bf5b54706cc502f45a), ставился с даунгрейдом поверх отладочной 1909. Прошло всё. Замена пакета подняла восстановление сама. Подключение из UI, перезагрузка (tun0 стоит на +15с после boot_completed), свайп из недавних. «Отключить» погасило туннель, и контрольный ребут его не вернул, как и задумано. Журнал приложения на релизной сборке через run-as недоступен, вердикт по tun0 и процессу. Лог прогона /tmp/xr279-release-control.log.
