# XR-191: Реализация LLD-37: long-poll доставки пресета: ручка ожидания /presets/{name}/wait на хабе (поколение версий на watch-канале, 200 полным пресетом либо 304 по таймауту), PresetCache::wait_for_update и общий watch_loop в xr-core вместо дублей циклов в xr-client и движке, откат в опрос с backoff при недоступности

Дизайн: [LLD-37](../lld/37-preset-longpoll.md), сценарий проверки в разделе 8.

## Ход работы

- Исполнение: субагент opus/medium по вердикту pick (квота: week_all 84% профицит, снимок 4ч 13м назад, протух, сдвига нет), 2026-08-03.
- Реализовано по LLD-37: ручка `GET /api/v1/presets/{name}/wait` и поколение
  пресетов на watch-канале в хабе, `PresetCache::wait_for_update` и общий
  `watch_loop` в xr-core, xr-client и движок Android переехали на него,
  деградированный опрос с backoff остался за `refresh_interval_secs`.
  Тесты: 5 на ручку хаба (включая пробуждение висящего запроса публикацией),
  6 на клиентскую половину (разбор 200/304/HTML-заглушки, версия в запросе,
  доставка правил циклом, откат в опрос, расписание backoff). Мутация
  «не будить ожидающих» проверена руками: снятый bump поколения роняет
  `wait_wakes_up_on_update` на 304 вместо 200.
- Статус LLD переведён в «Реализовано», отличия от дизайна (тесты цикла на
  событиях вместо мок-часов) записаны там же, факты уехали в ARCHITECTURE.md
  (разделы 5.3, 6.2, строка LLD-37).
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 84% профицит, снимок 4ч 31м назад, протух, сдвига нет), 2026-08-03.

## Ревью

- Побочный эффект bump_generation проверен мутацией только для update_preset (wait_wakes_up_on_update): снятие вызова в create_preset или delete_preset ни один тест не роняет, там нет ни одной проверки пробуждения/поколения по этим веткам: исправлено, create_preset_bumps_generation и delete_preset_wakes_waiter_with_404: снятие bump в любой из веток роняет свой тест
- Комментарии xr-core/src/session.rs:55 и xr-client/src/proxy.rs:53 всё ещё утверждают, что запись в RwLock<Arc<Router>> случается раз в hub_refresh_interval_secs/refresh_interval_secs - после long-poll это неверно (запись теперь на пробуждении ручки wait, интервал остался только для деградированного опроса), ARCHITECTURE.md эту фразу уже поправил, а эти два комментария нет: исправлено, оба комментария говорят про запись на пробуждении ручки ожидания, интервал остался паузой деградированного опроса
- Потолок WAIT_MAX_SECS (60 с) в wait_preset ничем не проверен: все тесты дают timeout_secs<=55, снятие или сдвиг .min(WAIT_MAX_SECS) ни один тест не роняет, а по дизайну это защита от промежуточных прокси, рвущих долгий ответ: исправлено, потолок вынесен в wait_hold и покрыт wait_hold_is_capped_and_has_default, снятие .min роняет тест
- README.md, раздел «Как происходит обновление» (строки 267, 277-298) и ссылка на него в строке 384, ещё описывают доставку правил старым пятиминутным опросом GET /api/v1/presets с 304 по ETag и обещают задержку «максимум refresh_interval_secs» - после LLD-37 это неверно (доставка long-poll'ом за секунды, опрос остался только деградированным режимом), а раздел не тронут этой веткой: исправлено, README переписан под long-poll: шаги обновления, секунды вместо пятиминутки, отдельный абзац про откат в опрос и комментарий у refresh_interval_secs
- README.md правку про long-poll сделал не до конца: раздел «Централизованные пресеты» перед фиксом (строка 224 «раз в несколько минут в фоне», строка 226 кейс «через 5 минут все 10 роутеров работают по новым правилам», строка 238 подпись стрелки в ASCII-схеме «GET раз в 5 мин») всё ещё держит старую пятиминутку прямо перед разделом «Как происходит обновление», который её опровергает - те же полминуты/секунды из фикса им противоречат: исправлено, верх раздела и схема переписаны: висящий запрос вместо опроса раз в несколько минут, кейс про пару секунд, подпись стрелки long-poll GET /wait (55 с)

## Сценарий проверки

Агентский, гоняется сразу после выката (хаб на проде уже с новой ручкой):

1. `curl -s -o /dev/null -w '%{http_code} %{time_total}\n' 'https://<хаб>/api/v1/presets/russia/wait?version=999999'` отвечает `200` мгновенно: версия не совпала, пресет уходит сразу.
2. `curl` с настоящей текущей версией и `timeout_secs=3` висит около трёх секунд и отдаёт `304`.
3. `curl` с несуществующим именем пресета отдаёт `404`.

Пользовательский, из раздела 8 [LLD-37](../lld/37-preset-longpoll.md), проверка по exit-IP, не по HTTP 200:

1. Добавить домен в пресет на хабе.
2. В пределах нескольких секунд, без рестартов и без кнопки «Обновить сейчас», новое соединение с LAN-устройства под роутером уходит через прокси (exit-IP сменился), ресурс открывается на Android.
3. Деградация: погасить хаб, убедиться, что туннель живёт, а клиент пишет про откат в опрос; поднять хаб и убедиться, что long-poll вернулся и следующее изменение доезжает мгновенно.

## Прогон после выката, 2026-08-03

Агентские шаги на боевом хабе `xr-hub.zoobr.top` (версия пресета была 36):

```text
--- 1. чужая версия, ждём мгновенный 200 ---
200 0.179233s
--- 2. своя версия, timeout_secs=3, ждём 304 около 3 с ---
304 3.177281s
--- 3. неизвестное имя, ждём 404 ---
404 0.178582s
```

Пользовательский шаг 2 прогнан живьём: висящий запрос на версии 36 стоял, пока
через три секунды заливали новый пресет, и проснулся ровно на публикации.

```text
--- PUT пресета ---
HTTP 200
--- висящий запрос вернул: 200 3.187232 (код, секунды) ---
версия из ответа ожидания: 37 | имён групп: 20 из 20
домены целы: 257 шт
```

Оба роутера подхватили новую версию тем же каналом, без рестарта:

```text
06:38:51 xr-client[13420]: INFO preset 'russia' v37 received from hub (long-poll)
06:38:51 xr-client[13420]: INFO preset 'russia' hot-swapped: new rules active without restart
06:38:51 xr-client[8219]:  INFO preset 'russia' v37 received from hub (long-poll)
06:38:51 xr-client[8219]:  INFO preset 'russia' hot-swapped: new rules active without restart
```

Проверка по exit-IP с LAN-машины под роутером DE: `ipinfo.io` из пресета уходит
через VPS, `ifconfig.me` мимо прокси. Адрес прямого выхода совпал с роутером,
адрес проксированного с основным VPS.

```text
INFO <LAN>:59334 -> <ifconfig.me>:443 [SNI: ifconfig.me] => Direct
INFO <LAN>:59335 -> <ipinfo.io>:443 [SNI: ipinfo.io] => Proxy
```

Шаг 3 (деградация) на проде не гонялся намеренно: он требует погасить боевой
хаб, за которым живут админка, инвайты и раздача APK. Ветка деградации закрыта
тестом `watch_loop_falls_back_to_polling_on_error` в xr-core.

## Выкат

- 2026-08-03 слито: a99d713, 49760d5, 7b65194, d19769b, 265835b, 5a9471f, feabf28, cd7e7e4, d46eb3e, f834f7b, 42eb5ad, 697591b, 2f32791, c5be9fa, bb7a2f9, fadc5d8
