# XR-228: Дрейф ARCHITECTURE.md: раздел 4.6 объявляет метод refreshLog() удалённым, а 7.2 того же документа его описывает и код в VpnViewModel.kt держит. Документ противоречит сам себе, поправить 4.6

## Ход работы

- Исполнение: субагент haiku/low по вердикту pick (квота: week_all 66%, снимок 46м назад, протух, сдвига нет), 2026-08-08.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 67%, снимок 2м назад, сдвига нет), 2026-08-08.

## Сценарий проверки

1. Проверить, что раздел 4.6 больше не говорит о удалении метода:
   ```bash
   grep -n "refreshLog()" docs/ARCHITECTURE.md | grep "удален"
   ```
   Команда не должна вывести ничего (раньше выводила строку 493).

2. Проверить, что раздел 4.6 теперь описывает метод refreshLog():
   ```bash
   grep -A 2 "который обновляется методом" docs/ARCHITECTURE.md | head -3
   ```
   Вывод должен содержать "refreshLog()" и "nativeJournalTail()".

3. Проверить консистентность с разделом 7.2:
   ```bash
   grep "refreshLog()" docs/ARCHITECTURE.md
   ```
   Должны быть две ссылки на метод: одна в 4.6 (новая, описание реализации), одна в 7.2 (описание использования).

4. Подтвердить существование метода в коде:
   ```bash
   grep -n "fun refreshLog()" xr-android/app/src/main/java/com/xrproxy/app/ui/VpnViewModel.kt
   ```
   Вывод должен показать определение метода на строке 335.

## Выкат

- 2026-08-08 слито: f395a27, 092ef1b
