# XR-269: Флак теста в xr-core --lib: при прогоне XR-113 один раз упал 1 тест из 153 (тайминг), повторные прогоны зелёные. Дифф задачи Rust не трогал. Найти тест с зависимостью от стенных часов и детерминировать.

записан 2026-08-08

## Черновик

Флак теста в xr-core --lib: при прогоне XR-113 один раз упал 1 тест из 153 (тайминг), повторные прогоны зелёные. Дифф задачи Rust не трогал. Найти тест с зависимостью от стенных часов и детерминировать.
