# XR-252: Браузерный вход к живому HTTP-сервису агента: HTTPS-вход на VPS поверх готового relay (LLD-23) с auth и пробросом WebSocket, чтобы дашборд devkit (DK-112) и будущие web-морды шар открывались с любого браузера без установки клиента. Смежно с фазой 3 LLD-33 (web-страница шары, weblink). Сейчас дашборд ходит через Tailscale; заводить целью, когда он живёт и канал станет узким местом.

## Черновик

Браузерный вход к живому HTTP-сервису агента: HTTPS-вход на VPS поверх готового relay (LLD-23) с auth и пробросом WebSocket, чтобы дашборд devkit (DK-112) и будущие web-морды шар открывались с любого браузера без установки клиента. Смежно с фазой 3 LLD-33 (web-страница шары, weblink). Сейчас дашборд ходит через Tailscale; заводить целью, когда он живёт и канал станет узким местом.
