# XR-230: У xr-hub нет health/readiness-эндпоинта: внешний аптайм-чек и логика переключения на standby дёргают содержательные ручки, которые не отделяют процесс жив от состояние поднято (hydrate загрузил инвайты/шары/ключ). Дешёвая ручка liveness/readiness упростит мониторинг и failover

## Что происходит

Внешний аптайм-чек и логика переключения на standby дёргают содержательные
ручки хаба (пресеты, инвайты). Такой ответ не отличает «процесс жив» от
«состояние поднято»: пока hydrate не загрузил инвайты, шары и ключ, хаб уже
отвечает на содержательные ручки, хотя половина функций ещё не готова.

## Чего хотим

Дешёвая ручка liveness/readiness, по которой мониторинг и failover судят
о хабе, не трогая содержательные ручки. Задача из состава цели XR-286,
конвейер идёт по её файлу.

## DoD

- `GET /healthz` без аутентификации отвечает `ok`, когда процесс жив;
  readiness отражает hydrate (инвайты, шары, ключ подписи) и отвечает
  неготовностью до его конца.
- Ручка покрыта тестом стенда xr-hub.
- Сквозная проверка после выката (адрес хаба из `.devkit/deploy.local`):
  `curl -s https://$HUB/healthz` печатает `ok`.

## Тесты

- Исключение: тесты (тесты в ветке есть, это три новых теста в модулях
  `#[cfg(test)]` файлов `xr-hub/src/api/health.rs`, `api/mod.rs` и
  `state.rs`, но ворот слияния узнаёт тестовые файлы по суффиксам
  `_test.go/.py/.sh`, а Rust-овские модульные тесты в тех же файлах
  не видит).

## Ранг

25+3+2+0+2 = 32, P2.

- Серьёзность 25 (Major): мониторинг и failover судят о хабе по содержательным
  ручкам, и неготовый хаб отвечает на них как живой. Обход дешёвый и очевидный:
  проверить другую ручку руками, поэтому не Critical.
- Неопределённость 3: подход выбран заранее, но в деталях были развилки: где
  живёт флаг готовности и что считать концом hydrate (инвайты, шары, ключ).
- Ценность 2: небольшое удобство, аптайм-чек и failover получают дешёвую
  ручку вместо разбора ответа содержательной.
- Поправка на баг 0: новая работа, ничего работающего не сломалось.
- Рычаг 2: небольшая своя польза, проверка после установки в HUB-DEPLOY и
  сценарии выката соседних задач хаба переходят на healthz.

## Ход работы

- Разработка: подпроцесс glm-5.3/medium по вердикту pick (квота: window5h_all 10%, week_all 20% профицит, снимок 0м назад, сдвига нет), 2026-08-29 09:32-09:32.
- regcheck: `regcheck --base main --inline xr-hub/src/api/mod.rs,xr-hub/src/state.rs -- cargo test -p xr-hub --bin xr-hub`, тесты краснеют на main, проходят на ветке, 2026-08-29.

## Ревью

- HUB-DEPLOY, раздел «6. Проверка»: проверка после установки продолжает дёргать содержательную ручку (curl -k https://localhost:8080/api/v1/presets). Задача заведена ровно из-за этого шаблона. Первой проверкой сделать curl -k https://localhost:8080/healthz с ожиданием ok. Проверку пресетов оставить второй строкой, как проверку содержимого.: исправлено
- В файле задачи нет раздела «Сценарий проверки», а без него задачу не пускают ни в Check, ни в слияние. Завести шаг, который гоняется после выката и не повторяет тесты ветки. Такими шагами могут быть curl -s https://$HUB/healthz с ожиданием ok и curl -s https://$HUB/readyz с ожиданием ready. Адрес хаба берётся из .devkit/deploy.local.: исправлено
- Раздел «Ранг» пуст, хотя TASKFORM требует его заполнить. Первой строкой поставить формулу и бакет из строки доски (25+3+2+0+2 = 32, P2). Ниже дать причину на каждое слагаемое по RANKING.md.: исправлено
- В «Ход работы» нет следа прогона regcheck. Ревью прогнало его по inline-регионам api/mod.rs и state.rs против main, тесты краснеют на старом коде и проходят на ветке. След дописать в раздел.: исправлено

## Сценарий проверки

Агентский, после выката (адрес хаба из `.devkit/deploy.local`):

1. `curl -s https://$HUB/healthz` печатает `ok`.
2. `curl -s https://$HUB/readyz` печатает `ready`.

## Выкат

- 2026-08-29 слито: 592e95ae, 65a958cf, 775692b2, e8ed411b
