# XR-236: Флаки-тест write_walk_verifies_agent_then_mutates_past_wrong_host в xr-core: мок-агент отвечает на одно соединение и закрывает его, reqwest переиспользует сокет под PUT и ловит network error. Рандомно валит cargo test --workspace и блокирует слияние с выкатом

## Из черновика XR-269 (записан 2026-08-08)

### Черновик

Флак теста в xr-core --lib: при прогоне XR-113 один раз упал 1 тест из 153 (тайминг), повторные прогоны зелёные. Дифф задачи Rust не трогал. Найти тест с зависимостью от стенных часов и детерминировать.

### Что показал грумминг 2026-08-10

Догадка про стенные часы проверкой не подтвердилась. Прогон `cargo test -p
xr-core --lib` сорок раз подряд на загруженной машине (десять `yes` в фоне)
дал три падения, и оба виновника сидят не на часах, а на HTTP-моке:

- `sync::tests::write_walk_verifies_agent_then_mutates_past_wrong_host`,
  прогоны 24 и 29, паника на `xr-core/src/sync.rs:3419` с текстом
  `upload should reach the correct agent: "network: error sending request for
  url (http://127.0.0.1:.../S/file/a.txt)"`;
- `sync::tests::test_upload_roundtrip`, прогон 36, паника на
  `xr-core/src/sync.rs:2299`, то есть `upload_file` вернул ошибку.

Ошибка одна и та же, `error sending request`, и это ровно тот симптом, что
описан в строке XR-236 и в закрытом XR-142.

Тесты с зависимостью от реального времени в крейте есть, и разведка их
перечислила: `dns::tests::test_expired_entry` (TTL кеша 1 мс и `lookup` сразу
после `allocate`), свёртка дублей в `journal.rs` и `stats.rs` (в сравниваемую
строку вшит timestamp с точностью до секунды), порог
`elapsed() < 500 ms` в `session::tests::peek_sni_gives_up_on_missing_tail`,
потолки `timeout(5 s)` в тестах `presets.rs`. Ни один из них за сорок прогонов
под нагрузкой не упал, поэтому отдельной задачей они пока не становятся: если
красное однажды придёт оттуда, начинать надо с этого списка.

Важное для исполнителя XR-236: `test_upload_roundtrip` это возврат закрытого
XR-142. Коммит `c189ad7` научил `serve_capture` дочитывать тело запроса, но
падение осталось, значит корень не в недочитанном теле. Мок по-прежнему
принимает ровно одно соединение (`listener.accept()` один раз), отвечает с
`connection: close` и закрывает сокет, а клиент reqwest держит пул и под
нагрузкой уходит писать в закрывающееся соединение. Чинить надо мок целиком,
на всё семейство `serve_capture` / `serve_capture_json`, а не по одному тесту.
Судить починку прогоном в цикле под нагрузкой, единичный зелёный прогон здесь
ничего не доказывает.

## Развилки

Чинить ли мок точечно под `upload_file` или всё семейство сразу. Взято
семейство. Тем же одним чтением и тем же закрытием сокета живут `serve_agent`,
`serve_healthz_then`, `serve_share`, `serve_once` и мок пробы живости, и любой
из них ловит ту же гонку, стоит запросу прийти двумя сегментами. После
перехода на общий приём в цикле `serve_once` и `serve_manifest_forever` ничем
не отличались друг от друга и слились в один `serve_canned`.

Как закрывать соединение. Мало дочитать тело. Сокет, закрытый с неразобранными
байтами в приёмном буфере, ядро гасит через RST, и клиент теряет уже полученный
ответ. `answer_and_close` сначала пишет ответ, потом шлёт FIN своей
половины и только затем дочитывает остаток с потолком в две секунды.

Держать ли keep-alive вместо `connection: close`. Не стали. Ответы моков
объявляют закрытие, и клиент не кладёт такое соединение в пул. Хватило приёма
в цикле.

## Ход работы

- Разработка: opus/medium по вердикту pick, 2026-08-25 01:00-01:00.
- Правка в тестовом модуле `xr-core/src/sync.rs`. `read_http_request` разбирает
  chunked, `answer_and_close` закрывает соединение без RST, а
  `spawn_http_mock` принимает соединения в цикле. Все моки файла переведены на
  него.
- Исключение: тесты (весь дифф лежит в тестовом модуле внутри
  `xr-core/src/sync.rs`, отдельного тестового файла у него нет). Тестов в
  правке три, `mock_drains_streamed_body_before_answering`,
  `mock_answers_a_second_request_on_a_new_connection` и
  `mock_stays_silent_on_a_broken_chunked_body`.
- Исключение: regcheck (правка и тест лежат в одном файле, тестовый mod внутри
  `xr-core/src/sync.rs`, и утилита не нашла его закрывающую скобку). Краснота
  проверена руками. Два теста подсажены в worktree на родительский коммит и
  упали там оба с «network: error sending request», третий на коммите до правки
  по ревью повис на ожидании ответа и упал по таймауту.
- Вычитка: 1 файл, 5 правок, 0 пометок, 2026-08-27.
- Ревью: sonnet/high по вердикту pick, 2026-08-28. По второму замечанию мок
  молчит на запросе, который дочитать не удалось, и на это заведён тест
  `mock_stays_silent_on_a_broken_chunked_body`. Прежний разбор на нечитаемом
  размере куска ждал недостающих байтов до конца прогона, и тест краснеет там
  по этому ожиданию.

## Ревью

- Сценарий проверки целиком гонялся в дереве ветки, шага после слияния в main не было. По ACCEPTANCE.md агентскому виду нужен шаг, который что-то доказывает про слитый код, а не повтор тестов ветки. Дописать стресс-прогон `cargo test -p xr-core --lib` на main: исправлено
- Мелочь. В `read_http_request` нечитаемый размер куска шёл за нулевой, и разбор тела обрывался без единого слова о поломке. На вход от reqwest это не бьёт, но диагностику битого клиента такой мок съедает. Не блокирует: исправлено

## Сценарий проверки

1. Краснота на старом коде. Завести worktree на коммите до правки
   (`git worktree add /tmp/xr236-base <коммит>`), подсадить в его
   `xr-core/src/sync.rs` перед строкой `// -- import path (LLD-29)` два теста
   `mock_drains_streamed_body_before_answering` и
   `mock_answers_a_second_request_on_a_new_connection` из правки и прогнать
   `cargo test -p xr-core --lib "sync::tests::mock_"`. Ожидание: оба теста
   падают, причём ровно с симптомом задачи, `network: error sending request
   for url`.
2. Те же тесты на новом коде. Прогнать `cargo test -p xr-core --lib
   "sync::tests::mock_"` в дереве задачи. Ожидание: `test result: ok. 2 passed`.
3. Флак не воспроизводится. Сорок прогонов `cargo test -p xr-core --lib` подряд
   на загруженной машине (восемь `yes > /dev/null` в фоне), как в грумминге.
   Ожидание: ни одного красного прогона, ни разу не всплывают
   `write_walk_verifies_agent_then_mutates_past_wrong_host` и
   `test_upload_roundtrip`.
4. Весь workspace цел. Прогнать `cargo test --workspace` и проверку на предупреждения
   `cargo test --workspace 2>&1 | grep "warning:" | grep -v "generated"`.
   Ожидание: тесты зелёные, вывод грепа пуст.
5. После слияния, в основном чекауте на main: сорок прогонов
   `cargo test -p xr-core --lib` подряд под той же нагрузкой из восьми фоновых
   `yes > /dev/null`. Ожидание: сорок зелёных прогонов подряд на слитом коде,
   ни одного `error sending request` в логах прогонов.

## Выкат

- 2026-08-28 слито: 194c6cf2, 90d96eec, d7cbc47e, 6b2c2940

## Проверка

Шаг 1, краснота на старом коде. Worktree на коммите до правки, туда подсажены
новые тесты:

```
thread 'sync::tests::mock_answers_a_second_request_on_a_new_connection' panicked at xr-core/src/sync.rs:2356:14:
вторая операция на тот же адрес: "network: error sending request for url (http://127.0.0.1:58954/S/file/b.txt)"
thread 'sync::tests::mock_drains_streamed_body_before_answering' panicked at xr-core/src/sync.rs:2338:14:
streamed upload must survive the mock: "network: error sending request for url (http://127.0.0.1:58955/S/file/big.bin)"
test result: FAILED. 0 passed; 2 failed
```

Третий тест на коммите до правки по ревью упал по ожиданию ответа:

```
thread 'sync::tests::mock_stays_silent_on_a_broken_chunked_body' panicked at xr-core/src/sync.rs:2462:14:
мок не разошёлся с клиентом за пять секунд: Elapsed(())
test result: FAILED. 0 passed; 1 failed
```

Шаг 2, те же тесты на новом коде:

```
test sync::tests::mock_stays_silent_on_a_broken_chunked_body ... ok
test sync::tests::mock_answers_a_second_request_on_a_new_connection ... ok
test sync::tests::mock_drains_streamed_body_before_answering ... ok
test result: ok. 3 passed; 0 failed
```

Шаг 3, сорок прогонов под нагрузкой на ветке: сорок зелёных, красных нет.

Шаг 4, весь workspace (на маке без `xr-client`, он тут не собирается): все
наборы зелёные, 786 тестов, падений нет. Греп по `warning:` пуст.

Шаг 5, сорок прогонов на main после слияния, восемь фоновых `yes` под
нагрузкой:

```
Зелёных прогонов: 40/40
test result: ok. 221 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.44s
grep -l "error sending request" /tmp/xr236-main-*.log | wc -l -> 0
```
