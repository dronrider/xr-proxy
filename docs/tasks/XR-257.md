# XR-257: Экран информации о файле шары: источник, ссылка на канал, всё известное о файле

Задача цели [XR-253](XR-253.md), пункт 2 постановки. Метаданные до приложения
уже доезжают (XR-255), макет утверждён владельцем (XR-254).

## Что есть сейчас

Долгий тап по строке файла открывает диалог «Файл» из трёх строк: путь, размер
и обрезанный SHA-256
([FilesScreen.kt:779](../../xr-android/app/src/main/java/com/xrproxy/app/ui/files/FilesScreen.kt#L779)).
Ни даты, ни признака просмотра, ни тем более источника там нет, а метаданные
импорта в такой диалог не влезают.

Агент с версии `xr-share-v0.3.8` отдаёт у строки манифеста необязательное поле
`meta` (`url`, `source`, `source_url`, `published`, `title`): у импортированного
по ссылке оно заполнено, у скачанного до этой цели там либо одна ссылка на
ролик, собранная из суффикса `[id]` в имени, либо ничего. Поле едет под
подписью манифеста и уже доступно на стороне Kotlin через JNI.

## Что сделать

Диалог заменяется экраном по фрейму 7 (файл с источником) и фрейму 8 (файл без
метаданных) страницы «Проводник шары» проекта Claude Design «Мобильное
приложение шар».

1. **Экран вместо диалога.** Открывается тем же долгим тапом и вдобавок обычным
   тапом по нескачанному файлу: сейчас такой тап не делает ничего, а скачанный
   файл по-прежнему открывается сразу. Имя показывается целиком, обрезать его
   на своём экране нечем и незачем.
2. **Блок «Файл»**: путь в шаре, размер, дата, признак просмотра, начало
   SHA-256. Это то, что показывал диалог, плюс дата и просмотр.
3. **Блок «Откуда файл»**: страница, откуда скачано, и канал автора, обе строки
   с адресом и обе открываются в браузере. Блок не прячется у файла без
   метаданных, а говорит прямым текстом, почему он пуст: спрятанный блок не
   отличить от «экран не умеет это показывать».
4. **Действия** переезжают на экран: открыть или скачать, удалить с устройства,
   удалить из шары. Последнее по-прежнему видно только с правом записи в
   токене и по-прежнему спрашивает подтверждение (XR-250), эту защиту не
   ронять.

Тексты в макете сняты с кода дословно, менять их не надо.

## Сценарий проверки

Агентский, на эмуляторе. Стенд поднимается по
[local-docs/android-stand/README.md](../../local-docs/android-stand/README.md),
снапшот эмулятора `xr-ready`, приёмы работы с `uiautomator` в памяти проекта.
В шаре стенда нужен файл, импортированный через агента по ссылке (метаданные
заполнены целиком), файл с суффиксом `[id]` в имени (одна ссылка, канал пуст) и
файл без метаданных вовсе.

1. У импортированного файла экран информации показывает источник и канал,
   адреса видны целиком.
2. Тап по ссылке канала уводит в браузер: проверяется тем, что система
   поднимает `Intent` (в дереве появляется окно выбора или браузер), а не тем,
   что «строка выглядит как ссылка».
3. У файла, скачанного до этой цели, тот же экран показывает либо одну ссылку
   на ролик без канала, либо честный текст про то, что источник неизвестен.
4. Удаление из шары с экрана по-прежнему спрашивает подтверждение, а без права
   записи в токене этого действия на экране нет.

Вывод команд и снимки дерева класть в файл задачи реальные, а не пересказом.

## Ход работы

- Исполнение: субагент opus/high по вердикту pick (квота: week_all 20%, снимок 38м назад, сдвига нет), 2026-08-05.

### Сделано

- `ManifestEntry` в
  [Share.kt](../../xr-android/app/src/main/java/com/xrproxy/app/model/Share.kt)
  разбирает поле `meta` (тип `FileMeta`) и пишет его обратно в JSON: тем же
  методом сохраняется кэш листинга (XR-099), и без источника офлайн-заход терял
  бы ссылку на канал.
- `FileInfoScreen` в
  [FilesScreen.kt](../../xr-android/app/src/main/java/com/xrproxy/app/ui/files/FilesScreen.kt)
  вместо диалога «Файл»: заголовок с полным именем, подпись из размера, даты и
  состояния, карточки «Файл» и «Откуда файл», кнопка «Открыть» либо «Скачать» и
  действия удаления. Ссылки уводят в браузер через `ACTION_VIEW`.
- Открытый экран это `detailsPath` в `FilesViewModel`, а не локальный стейт:
  строку экран берёт из живого манифеста, поэтому удалённый файл закрывает его
  сам, `navigateUp` проходит экран, папку и шару одной кнопкой, а системный
  «Назад» остаётся одним обработчиком на весь экран файлов.
- Тап по нескачанной строке ведёт на этот экран (раньше не делал ничего),
  долгий тап по любой строке тоже.
- Дока: абзац про экран в разделе «Откуда взялся файл (XR-255)»
  [ARCHITECTURE.md](../ARCHITECTURE.md), там же поправлено место удаления из
  шары (XR-250).

Два отступления от макета, оба вынужденные:

- строка «Импорт: дата, качество до 1080p» убрана, вместо неё «Опубликовано» из
  `meta.published`. Ни даты импорта, ни выбранного качества агент не хранит
  (XR-255 закрепил пять полей), а сочинять их экран не станет;
- колонка ключа 96dp вместо 74dp: «Опубликовано» в макетной ширине переносилось
  на вторую строку.

### Проверка

Стенд из [local-docs/android-stand](../../local-docs/android-stand/README.md),
эмулятор `xr-test` со снапшотом `xr-ready`, отладочный APK из `build.sh`. Шара
стенда переведена в писчую с импортом (иначе токен только на чтение), файл с
полными метаданными заведён импортом через агента с плагином-заглушкой:

```
$ curl -s -X POST -H "Authorization: Bearer $TOK" -H 'content-type: application/json' \
    -d '{"url":"https://www.youtube.com/watch?v=aBcDeFgHiJk","dest":""}' \
    http://127.0.0.1:18443/Ga3rpUDS74s0LBDL65b4zw/import
{"job_id":"3a26a7b4d89e97c8"}
$ curl -s -H "Authorization: Bearer $TOK" \
    http://127.0.0.1:18443/Ga3rpUDS74s0LBDL65b4zw/import/3a26a7b4d89e97c8
{"state":"done","progress":100.0,"files":["Как перестать откладывать. Полная версия [aBcDeFgHiJk].mp4"]}
$ cat local-docs/android-stand/share/.xr-meta.json
{
  "files": {
    "Как перестать откладывать. Полная версия [aBcDeFgHiJk].mp4": {
      "url": "https://www.youtube.com/watch?v=aBcDeFgHiJk",
      "source": "Ясно и по делу",
      "source_url": "https://www.youtube.com/@yasno-i-po-delu",
      "published": "2026-07-28",
      "title": "Как перестать откладывать и начать делать, часть 2"
    },
    "видео/Старый ролик про NAT и пробитие портов [dQw4w9WgXcQ].mp4": {
      "url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ"
    }
  }
}
```

**1. Источник и канал у импортированного файла.** Тап по строке в проводнике,
дерево экрана (`uiautomator dump`, текст и границы):

```
‹ Назад [53,189][180,242]
Как перестать откладывать. Полная версия.mp4 [85,299][995,421]
42.0 КБ · 8/5/26 [85,442][331,505]
ФАЙЛ [127,579][214,642]
Путь [127,674][363,719]
Как перестать откладывать. Полная версия [aBcDeFgHiJk].mp4 [379,674][953,764]
Размер [127,786][363,831]
42.0 КБ [379,786][499,831]
Дата [127,853][363,898]
8/5/26 [379,853][485,898]
Просмотр [127,920][363,965]
Не просмотрено [379,920][644,965]
SHA-256 [127,987][363,1032]
bf4735dab774b445... [379,987][746,1032]
ОТКУДА ФАЙЛ [127,1154][338,1217]
Страница [127,1249][363,1294]
Как перестать откладывать и начать делать, часть 2 [379,1249][953,1339]
https://www.youtube.com/watch?v=aBcDeFgHiJk [379,1347][953,1427]
Канал [127,1449][363,1494]
Ясно и по делу [379,1449][618,1494]
https://www.youtube.com/@yasno-i-po-delu [379,1502][953,1582]
Опубликовано [127,1604][363,1649]
7/28/26 [379,1604][504,1649]
Скачать [462,1786][619,1839]
Удалить из шары [389,1918][691,1971]
```

Адреса видны целиком: обе ссылки занимают по две строки высотой 80 пикселей
(`[379,1347][953,1427]`), то есть переносятся, а не обрезаются.

**2. Тап по ссылке канала уводит в браузер.** Логкат чистится перед тапом,
дальше тап по строке канала:

```
$ adb logcat -c && adb shell input tap 600 1540 && sleep 8 && adb logcat -d | grep "ActivityTaskManager: START"
08-05 00:34:13.085 I ActivityTaskManager: START u0 {act=android.intent.action.VIEW
  dat=https://www.youtube.com/... flg=0x10000000 cmp=com.google.android.youtube/.UrlActivity}
  with LAUNCH_MULTIPLE from uid 10207 (BAL_ALLOW_VISIBLE_WINDOW) result code=2
```

На прогоне до этого ссылку взял Chrome, и в его адресной строке видно ровно тот
адрес, что стоял на экране:

```
$ adb shell dumpsys activity activities | grep topResumedActivity
    topResumedActivity=ActivityRecord{621d4f2 u0 com.android.chrome/...FirstRunActivity t14}
$ uiautomator dump после первого запуска Chrome
Connection is secure [136,178][199,241]
m.youtube.com/@yasno-i-po-delu [210,144][671,275]
```

**3. Файл, скачанный до этой цели.** У файла с ютуб-суффиксом одна ссылка и
никакого канала, у файла без метаданных честный текст:

```
Старый ролик про NAT и пробитие портов.mp4 [85,299][995,421]
...
ОТКУДА ФАЙЛ [127,1154][338,1217]
Страница [127,1249][321,1294]
https://www.youtube.com/watch?v=dQw4w9WgXcQ [321,1249][953,1329]
Скачать [462,1466][619,1519]
```

```
Заметка.txt [85,299][358,360]
...
ОТКУДА ФАЙЛ [127,1048][338,1111]
Неизвестно. Файл появился в шаре раньше, чем приложение стало запоминать
  ссылку, и восстановить её нечем. [127,1132][953,1276]
Скачать [462,1402][619,1455]
```

**4. Удаление из шары.** Оба дерева выше сняты с токеном без права записи, и
строки «Удалить из шары» на экране нет. После write-привязки инвайта действие
появляется и спрашивает подтверждение:

```
Удалить из шары? [183,946][728,1030]
«Как перестать откладывать. Полная версия [aBcDeFgHiJk].mp4» пропадёт у всех,
  у кого есть доступ к шаре, и с этого устройства. Вернуть файл сможет только
  владелец. [183,1072][897,1337]
Отмена [502,1437][634,1490]
Удалить [719,1437][865,1490]
```

Подтверждение доведено до конца: файл ушёл из шары (`DELETE share=... rel=Как
перестать откладывать. Полная версия [aBcDeFgHiJk].mp4` в логе агента), а экран
информации закрылся сам, потому что строка пропала из манифеста.

У скачанного файла экран показывает свой набор действий:

```
Просмотр [127,1026][363,1071]
Просмотрено [379,1026][597,1071]
Открыть [456,1614][624,1667]
Удалить с устройства [351,1746][729,1799]
Удалить из шары [389,1872][691,1925]
```

`cargo test --workspace --exclude xr-client` зелёный (на маке `xr-client` не
собирается), сборка APK без новых предупреждений. Автотестов на Android нет по
правилу проекта.

### Наблюдение мимо задачи

Импорт по URL из самого приложения на локальном стенде до агента не доходит: в
логе агента запроса нет, на экране ни ошибки, ни строки прогресса. Тот же
импорт curl-ом по тому же токену отрабатывает. Похоже на первый недоступный
адрес-кандидат гранта (в стенде это LAN-адрес хоста), но проверено это не было.
Кандидат в отдельную строку доски.
- Ревью: субагент sonnet/high по вердикту pick (квота: week_all 21%, снимок 23м назад, сдвига нет), 2026-08-05.
