# xr-proxy: задачи (префикс XR)

Канбан-доска открытой работы и единственный источник правды по тому, что в работе.
Подробные разборы лежат в [docs/lld/](lld/) и в гитигнорнутом `local-docs/`, доска их
только индексирует, описания не дублирует. Готовую задачу из доски переносим в
[docs/TASKS-archive.md](TASKS-archive.md). ID сквозные по репозиторию и не
переиспользуются; ID ставим в subject коммита там, где уместно (`feat(core): XR-9 ...`).
Чувствительное (IP, доступы, инфра) в доску не пишем, оно остаётся в `local-docs/`.

Приоритеты: P0 чинить немедленно, P1 мешает ежедневному использованию, P2 заметно
улучшает, P3 терпит. Типы: `bug` / `task` / `LLD`.

## In progress

Нет.

## Check (готово, ждёт проверки пользователем)

| ID | Задача | Тип | P | Ссылка |
|--------|--------|-----|---|--------|
| XR-043 | Директория хранения шары: выбор, смена и миграция. Дизайн (выбран MANAGE_EXTERNAL_STORAGE): дефолт это app-dir без разрешений; своя папка на общем хранилище пишется движком напрямую (реальный FS-путь, инвариант синка цел), доступ ко всем файлам запрашивается через системные Настройки только при выборе своей папки (Android 11+). Шара кладётся в свой подкаталог `<папка>/<имя>`, чтобы true-mirror delete не затронул чужие файлы рядом. Миграция (`migrate_dir` в xr-core, юнит-тесты): rename на том же томе, copy+remove с проверкой места между томами, коллизии (идентичный пропустить, иной конфликт), `.xrsync-part` отбрасываются, старая папка вычищается. Проверка: (1) добавить шару, тапнуть файл -> диалог выбора папки, выбрать «Папка приложения» -> качает в app-dir, повторно не спрашивает; (2) у шары с уже скачанным нажать иконку папки -> «Своя папка…» -> выдать доступ -> выбрать папку -> файлы переезжают в `<папка>/<имя>` (видны в проводнике телефона), синк показывает +0 (не перекачивает), старое вычищено; (3) сменить папку ещё раз -> снова переезд без перекачки; краевые: на Android 10 «Своя папка» недоступна (подсказка 11+), выбор SD -> подсказка про основное хранилище | task | P2 | [data/ShareRepository.kt:32](../xr-android/app/src/main/java/com/xrproxy/app/data/ShareRepository.kt#L32) |

## Backlog

| ID | Задача | Тип | P | Ссылка |
|--------|--------|-----|---|--------|
| XR-009 | Авто-переключение роутера на запасной сервер при падении основного (пул из нескольких VPS) | LLD | P1 | [docs/lld/10-router-multi-vps-failover.md](lld/10-router-multi-vps-failover.md) |
| XR-010 | Мониторинг, уведомления и панель здоровья | LLD | P1 | [docs/lld/11-monitoring-health-panel.md](lld/11-monitoring-health-panel.md) |
| XR-049 | В доверенной сети «Включить здесь» не подключает. Резюм (`resumeOnTrustedNetwork` -> `resumeOverride` -> `requestResume`) поднимает туннель только из фазы `Paused` (`if phase != Paused return`), из Disconnected (напр. по нотификации) это no-op; плюс возможно `overrideNetwork` не уважается авто-паузой и watcher сразу возвращает паузу. Ожидаемо: туннель поднимается на текущей доверенной сети и не уходит обратно в паузу. Шаги: в доверенной Wi-Fi нажать «Включить здесь» -> ожидается подключение | bug | P1 | [service/XrVpnService.kt:320](../xr-android/app/src/main/java/com/xrproxy/app/service/XrVpnService.kt#L320), [ui/VpnViewModel.kt:470](../xr-android/app/src/main/java/com/xrproxy/app/ui/VpnViewModel.kt#L470) |
| XR-012 | Информативный индикатор соединения вместо смайлика | task/LLD | P2 | `local-docs/problems.md` (10) |
| XR-013 | Редактор правил проксирования на хабе (гибридный): TOML как источник правды, пофрагментные правки в админке с сохранением комментариев, компиляция в раздаваемый JSON. Общая модель правил с XR-047 | LLD | P2 | [docs/lld/14-hub-hybrid-rules-editor.md](lld/14-hub-hybrid-rules-editor.md) |
| XR-015 | Развёртывание нового сервера и роутера в один клик (по SSH из приложения: установка прокси, имя сети, минимум ручных шагов) | LLD | P2 | [docs/lld/13-zero-touch-provisioning.md](lld/13-zero-touch-provisioning.md) |
| XR-016 | Раздельный туннель по приложениям (выбрать, какие приложения идут через прокси, остальные напрямую) | LLD | P2 | [docs/lld/07-android-per-app-tunnel.md](lld/07-android-per-app-tunnel.md) |
| XR-020 | Живые правила из хаба для серверов, добавленных вручную | LLD | P2 | [docs/lld/16-manual-server-hub-rules.md](lld/16-manual-server-hub-rules.md) |
| XR-025 | Хаб-реестр роутеров: регистрация роутера и его личность, статус наверх (роутер сам опрашивает хаб), раздел «Роутеры» в админке (онлайн, версия, когда был на связи). Хаб знает и видит свой парк роутеров. Поверх него удалённое управление в XR-048 | LLD | P2 | [docs/lld/17-hub-router-registry.md](lld/17-hub-router-registry.md) |
| XR-032 | QR на странице инвайта `/view` кодирует относительный claim-путь (POST), приложение такое не парсит. Кодировать каноническую ссылку `https://<host>/invite/<token>` (хост берётся из payload.hub_url/конфига хаба) | bug | P2 | [xr-hub/src/api/invites.rs:76](../xr-hub/src/api/invites.rs#L76) |
| XR-033 | Установка приложения по инвайту: страница `/view` как воронка для получателя без приложения. Кнопка «Скачать APK» (latest с хаба) + краткая инструкция, кнопка «Открыть в приложении» (deep link `https /invite/` и `xr://`, intent-filter'ы в манифесте уже есть), рабочий QR (XR-032). Опционально `assetlinks.json` для авто-открытия App Links на офиц. хабе | task | P2 | [xr-hub/src/api/invites.rs:46](../xr-hub/src/api/invites.rs#L46) |
| XR-034 | Агент анонсирует приватный LAN-адрес (`192.168.x`), шара недоступна вне домашней сети. При `xr-share share` предупреждать, если резолвится приватный addr; задокументировать проброс `8443` + публичный IP/DDNS. Полноценный доступ без белого IP вынесен в XR-035 | task | P2 | [xr-share/src/cli.rs:47](../xr-share/src/cli.rs#L47) |
| XR-035 | Доступ к шаре без белого IP (агент за NAT/CGNAT): недостижимый снаружи агент остаётся доступен потребителю через посредника. Спроектировать архитектуру. Развилка: реверс-туннель агент -> хаб/relay (все байты идут через посредник, дорого по egress на шарах в десятки ГБ) против hole-punching агент <-> потребитель с relay как fallback (байты напрямую, экономит трафик, но CGNAT/симметричный NAT часто требует fallback). Учесть E2E (relay не видит плейнтекст), хаб как слой управления (сигналинг), а передача байт отдельным relay-сервисом, переиспользование обфусцированного mux из xr-core/xr-server как транспорта; целостность байт через подписанный манифест (XR-046). При проектировании LLD возможна разбивка на relay (работает всегда) и hole-punching (оптимизация трафика) | LLD | P2 | (LLD позже) |
| XR-041 | Уведомление о новой версии в рамках всего приложения, а не только на главной (VPN). Сейчас баннер обновления виден лишь на главном экране. Сделать индикацию доступной с любой вкладки: глобальное состояние «доступно обновление» + бадж/точка на иконке нижней навигации. Переработать отображение, один из вариантов это свести настройки в одно место и показывать бадж на иконке настроек (дизайн открыт). Решить, на какой иконке/в каком разделе показывать. Плюс сама авто-проверка ненадёжна: баннер часто не приходит, приходится проверять вручную (фидбек, подтверждено), сделать проактивную проверку надёжной | task | P2 | [xr-android/.../ui/update/UpdateBanner.kt](../xr-android/app/src/main/java/com/xrproxy/app/ui/update/UpdateBanner.kt), [UpdateManager.kt](../xr-android/app/src/main/java/com/xrproxy/app/update/UpdateManager.kt) |
| XR-042 | Логи не должны зависеть от состояния (сеть/режим), это просто журнал записей. Сейчас лента собирается из транзиентных источников (engine `recentErrors` + `probeLog`): при смене сети движок перезапускается и recentErrors обнуляется, после переключения доверенная -> обычная тянутся пробы доверенной. Сделать единый персистентный append-only журнал, переживающий перезапуск движка и смену сети/режима (не обнуляется): все источники пишут в один буфер. Само переключение сети/режима логировать отдельной записью. Ротацию (размер/кол-во) вынести в настройки. Лог должен включать и файловый трафик/события (скачивания, синки, ошибки шар), не только VPN (фидбек) | bug | P2 | [ui/VpnViewModel.kt:867](../xr-android/app/src/main/java/com/xrproxy/app/ui/VpnViewModel.kt#L867), [service/XrVpnService.kt](../xr-android/app/src/main/java/com/xrproxy/app/service/XrVpnService.kt) |
| XR-044 | Синк-UX неинтуитивен (фидбек): галочка `selection` (что зеркалить) и `localPaths` (что скачано) это две разные сущности, пользователь их путает; тап перегружен (и выбирает, и качает, и открывает), а при single-transfer lock второй тап во время загрузки молча делает `return` ([FilesViewModel.downloadAndOpen](../xr-android/app/src/main/java/com/xrproxy/app/ui/files/FilesViewModel.kt#L199)), поэтому «тапнул второй файл, он не качается». Редизайн: схлопнуть `selection` и `localPaths` в одну ось «файл на устройстве (или едет)». Заменить чекбокс на per-file контрол-кнопку с состояниями: не скачан -> зелёный `+` (тап ставит в общую очередь синка); качается -> прогресс на строке + действие «отмена» (тап отменяет, контрол всегда с действием, не мёртвый статус); скачан -> `−` (тап удаляет локальную копию); ошибка -> строка с красным фоном и сохранённым прогрессом на фоне + иконка «ретрай» (тап возобновляет с места). Тап по строке = открыть, но только для скачанного (open-on-tap убирает только скачивание-по-тапу). Несколько тапов кладут все файлы в очередь (чинит тихий `return`). Папки: tri-state по конвенции established-приложений (selective-sync/offline у Drive/Dropbox), тап докачивает недостающее, без самодельных правил. Видимый прогресс фонового синка на строках (сейчас полоска только для foreground). Развести «обновить список с хаба» и «синк/очередь» (криптичная иконка круговых стрелок путает оба смысла). Существующие `localPaths` мигрировать как selected. Смежно с XR-055 (та же карточка/раскладка) и XR-043 (папка хранения). Проверка: тапнуть `+` на трёх файлах подряд -> все три встают в очередь и качаются по очереди с прогрессом; отменить качающийся -> возвращается `+`; скачанный тапнуть -> открывается; `−` на скачанном -> файл удаляется, иконка -> `+`; оборвать сеть в процессе -> красная строка + ретрай, тап -> докачивает | task | P2 | [ui/files/FilesScreen.kt](../xr-android/app/src/main/java/com/xrproxy/app/ui/files/FilesScreen.kt), [ui/files/FilesViewModel.kt:199](../xr-android/app/src/main/java/com/xrproxy/app/ui/files/FilesViewModel.kt#L199) |
| XR-045 | Полностью ленивый хеш, снимает корень холодного кеша без прогрева и персиста: хеш считать по требованию при скачивании одного файла (для verify), синк-diff делать по size+mtime, а не по хешу. Прохода по всей шаре тогда нет. Холодное скачивание уже работает (verify пропускается при пустом хеше); осталось diff по size+mtime + убрать эагерный прогрев | task | P2 | [xr-core/src/sync.rs](../xr-core/src/sync.rs), [xr-share/src/manifest.rs](../xr-share/src/manifest.rs) |
| XR-046 | Подписанный манифест агента: сейчас агент раздаёт по HTTP и манифест НЕ подписан, поэтому проверка хеша не защищает от подмены (MITM меняет и файл, и хеш под него). Агент подписывает манифест своим ключом, потребитель проверяет подпись пиннутым `agent_pubkey` (уже приходит в гранте по инвайту), тогда verify файла осмыслен и ловит подмену + защищает relay-путь. Опц. TLS. Предпосылка для XR-035 (relay) и делает осмысленной XR-038 | LLD | P2 | [xr-share/src/server.rs](../xr-share/src/server.rs), [xr-share/src/manifest.rs](../xr-share/src/manifest.rs) |
| XR-047 | Редактор правил проксирования в приложении (Android): вкладка правил, пресет с хаба только для чтения + пользовательские правила сверху (срабатывают первыми), убрать хардкод PRESET_RUSSIA. Общая модель правил с XR-013 | LLD | P2 | [docs/lld/05-android-rules-editor.md](lld/05-android-rules-editor.md) |
| XR-048 | Удалённое управление роутерами (поверх реестра XR-025): подписанные команды через тот же исходящий канал (очередь команд на хабе, роутер проверяет подпись закреплённым ключом и применяет), ограниченный набор команд без произвольного shell | LLD | P2 | [docs/lld/20-router-remote-management.md](lld/20-router-remote-management.md) |
| XR-050 | Шара с несколькими адресами (LAN + публичный), потребитель выбирает достижимый. Сейчас у шары один addr: на одной LAN с агентом публичный IP недоступен из-за NAT hairpin (роутер не заворачивает LAN -> свой WAN-IP обратно на 192.168.x), а LAN-адрес агента недоступен снаружи. Хранить в записи шары список адресов (LAN-адреса агента + публичный/DDNS), потребитель пробует их по очереди и берёт первый ответивший: внутри LAN по LAN-IP без hairpin, снаружи по публичному. Роутеро-независимо, в отличие от включения NAT reflection на роутере | task | P2 | [xr-share/src/cli.rs](../xr-share/src/cli.rs), [xr-core/src/sync.rs](../xr-core/src/sync.rs) |
| XR-017 | Клиент под macOS | LLD | P3 | `local-docs/problems.md` (12) |
| XR-018 | Автопополнение правил проксирования из community-фидов | LLD | P3 | `local-docs/problems.md` (13) |
| XR-019 | Браузерное расширение | LLD | P3 | `local-docs/problems.md` (5b) |
| XR-026 | Метрики и графики по парку роутеров (история, тренды, алерты): хаб копит отчёты и отдаёт их в Prometheus-формате, поверх VictoriaMetrics + Grafana (панели скорость/аптайм/инциденты). Транспорт данных уже в XR-025, собираем только операционные метрики без слежки за доменами | LLD | P3 | [docs/lld/18-fleet-metrics-grafana.md](lld/18-fleet-metrics-grafana.md) |
| XR-030 | Аутентификация через инвайт: OIDC-флоу с email-OTP, привязанным к инвайту, и выдачей JWT для всей экосистемы; групповые инвайты (несколько email под подтверждение) | LLD | P3 | (LLD позже) |
| XR-036 | Self-service онбординг агента по инвайту: пользователь по инвайт-идентичности (поверх XR-030) сам получает reg-token/agent-credential и поднимает свой xr-share, без ручной выдачи токена админом. Скоупы инвайта `consume` / `publish`, не открытая регистрация | LLD | P3 | (LLD позже) |
| XR-038 | Персист кеша хешей нужен, когда хеши станут несущими: подписанный манифест (XR-046) и relay (XR-035). Тогда хеши обязательны и пересчитывать всю шару при каждом рестарте дорого. До этого не приоритет (ближний путь это ленивый хеш XR-045). Реализация: HashCache на диск (ключ path+size+mtime), грузить при старте | task | P3 | [xr-share/src/manifest.rs](../xr-share/src/manifest.rs) |
| XR-040 | Опционально BLAKE3 вместо SHA-256 для хеша манифеста: криптостойкий (в отличие от XXH3) и в 5-10x быстрее, многопоточный. Ускоряет первичный прогон на CPU-bound (SSD); на HDD упор в чтение, поэтому вторично. Криптогарантию сохранить (нужна для будущего relay/недоверенного транзита, XR-035) | task | P3 | [xr-share/src/manifest.rs:132](../xr-share/src/manifest.rs#L132) |
| XR-051 | Доступ к шаре на запись/редактирование. Сейчас шара read-only (агент только раздаёт). Добавить запись: грант на редактирование (отдельный скоуп от чтения, минтит хаб), агент принимает создание/запись/удаление файлов строго в пределах шары (safepath, как при раздаче). Предпосылка для импорта по URL (XR-052) и любых правок шары с устройства | LLD | P3 | (LLD позже) |
| XR-052 | Импорт контента по URL как плагин (поверх записи XR-051). UI: в writable-шаре зайти в папку -> кнопка «Импорт по URL» -> вставить ссылку на страницу -> агент качает контент в эту папку с адекватным именем из метаданных (напр. заголовок ролика). НЕ прибито к xr: агент держит общий интерфейс плагина-фетчера (вход {url, dest_dir}, выход прогресс + итоговый файл), референс-плагин это обёртка yt-dlp+ffmpeg, ядро xr-share остаётся тонким файлсервером без жёсткой зависимости; роутинг URL -> плагин по паттерну, плагины опциональны (владелец включает). Безопасность (агент в чужой квартире, URL от пользователя): dest строго в пределах шары, url литеральным аргументом без shell, SSRF-защита, лимиты размер/диск/таймаут, только write-скоуп, по возможности песочница плагина | LLD | P3 | (LLD позже) |
| XR-055 | Кривая компоновка карточки шары в списке «Мои шары»: название втискивается в узкую колонку и обрезается («Антон Назаров - О...»), хотя карточка уже трёхстрочная и место под имя есть. Причина (регрессия XR-043): один Row с ведущей иконкой + Column(weight 1f) + трейлинг-кластер из трёх контролов (иконка «папка хранения» + тумблер синка + корзина); кластер ~150dp плюс ведущая иконка ужимают колонку имени до ~110dp. Дать имени полную ширину без роста высоты блока: либо вынести заголовок в отдельную верхнюю строку на всю ширину, а метаданные (выбрано/папка) + контролы под ней; либо убрать третью иконку из кластера, сделав строку «Папка: ...» тапабельной для смены папки (заголовку больше места). Смежно с XR-044 (та же карточка/экран) | bug | P3 | [ui/files/FilesScreen.kt:273](../xr-android/app/src/main/java/com/xrproxy/app/ui/files/FilesScreen.kt#L273) |

## Blocked

Нет.
